#compdef responder

_arguments \
  '--version[Show programs version number and exit]' \
  '-h[Show this help message and exit]' \
  '-A[Analyze mode. This option allows you to see NBT-NS, BROWSER, LLMNR requests without responding]' \
  '-I[Network interface to use, you can use ALL as a wildcard for all interfaces]:interface' \
  '-i[Local IP to use (only for OSX)]:local IP' \
  '-6[Poison all requests with another IPv6 address than Responders one]:external IPv6' \
  '-e[Poison all requests with another IP address than Responders one]:external IP' \
  '-b[Return a Basic HTTP authentication. Default: NTLM]' \
  '-d[Enable answers for DHCP broadcast requests. This option will inject a WPAD server in the DHCP response. Default: False]' \
  '-D[This option will inject a DNS server in the DHCP response, otherwise a WPAD server will be added. Default: False]' \
  '-w[Start the WPAD rogue proxy server. Default value is False]' \
  '-u[Upstream HTTP proxy used by the rogue WPAD Proxy for outgoing requests (format: host:port)]:upstream proxy' \
  '-F[Force NTLM/Basic authentication on wpad.dat file retrieval. This may cause a login prompt. Default: False]' \
  '-P[Force NTLM transparently/Basic prompt authentication for the proxy. WPAD doesnt need to be ON. This option is highly effective. Default: False]' \
  '-Q[Tell Responder to be quiet, disables a bunch of printing from the poisoners. Default: False]' \
  '--lm[Force LM hashing downgrade for Windows XP/2003 and earlier. Default: False]' \
  '--disable-ess[Force ESS downgrade. Default: False]' \
  '-v[Increase verbosity]'
